pipeline {

    agent { label 'worker' }

    stages {
        stage('Test aws s3 ls') {
            steps {
                script {
                    sh "sudo aws sts get-caller-identity"
                }
            }
        }
    }
}